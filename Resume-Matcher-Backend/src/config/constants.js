export const DB_NAME = "resumematcher";

// OpenAI model used for resume-vs-JD scoring
export const AI_MODEL = "gemini-1.5-flash";

// Max resume file size accepted (in bytes) - 5MB
export const MAX_FILE_SIZE = 5 * 1024 * 1024;
